{
  "name": "ruswift-escrow-frontend-tests",
  "version": "1.0.0",
  "description": "Frontend tests for escrow logic",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}
